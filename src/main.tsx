import React from 'react';
import ReactDOM from 'react-dom/client';
import { ColorBox } from './components/atoms/ColorBox';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ColorBox>ddd</ColorBox>
  </React.StrictMode>,
);
